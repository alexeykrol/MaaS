# Контекст проекта MaaS для Claude Code

> Этот файл автоматически загружается в контекст каждой сессии Claude Code

---

## 🔄 Текущее состояние

> **🎉 MVP ЗАВЕРШЁН! Emulator готов!**

**Проект:** MaaS MVP (Memory as a Service)
**Прогресс:** MVP 100% + Emulator v0.3.1
**Статус:** Фокус на качестве MaaS

### ✅ Завершено:

**MVP (Steps 0-11):** Полный pipeline работает (NEW → COMPLETED)

**User Emulator v0.3.1:**
- Pipeline Mode: оба роли роутятся через MaaS
- Direct Mode: прямые вызовы OpenAI
- Role Instructions: [ROLE INSTRUCTION: ...] в pipeline
- localStorage Persistence: промпты сохраняются в браузере
- Export to Markdown: экспорт диалогов

**Analyzer v0.2:** LLM-based semantic keyword extraction

**Orchestrator:** Интегрирован в server.ts (одна команда: `npm run dev`)

### 🔍 Выводы тестирования:

| Компонент | Статус |
|-----------|--------|
| Pipeline логистика | ✅ Работает |
| Archivist | ✅ Сохраняет memories |
| Analyzer | ✅ Находит memories |
| Assembler | ✅ Включает в контекст |
| **Memory Utilization** | ⚠️ Требует улучшения |

### 🔜 Следующий фокус: MaaS Quality
- Улучшение использования memories в ответах
- Качество retrieval
- "Adversarial" тесты (факт только в LSM)

### 📐 Архитектура Self-Learning (отложено):
```
Пользователь → AGENT → Campaign → MANAGER → cycle(Emulator→Analyst→Teacher→Tuner) → MaaS
```

### 📊 Документация:
- См. **docs/selflearn/** — система самообучения

---

## 🤖 ВАЖНО ДЛЯ AI-АГЕНТА

**При ПЕРВОМ сообщении пользователя в новой сессии ОБЯЗАТЕЛЬНО:**

1. ✅ **Подтверди загрузку контекста:**
   ```
   ✅ CLAUDE.md загружен в контекст
   📂 Текущая директория: [показать pwd]
   ```

2. ✅ **Проактивно прочитай ключевые файлы:**
   - `PROJECT_SNAPSHOT.md` - текущий статус (ПРИОРИТЕТ!)
   - `PROJECT_INTAKE.md` - требования проекта
   - `BACKLOG.md` - план разработки по шагам

3. ✅ **Краткий анализ + допущения:**
   - Что понял о проекте (2-3 предложения)
   - Какие мета-файлы отсутствуют (без паники)
   - Сделай допущения о проекте на основе кода

4. ✅ **Задай 2-3 КРИТИЧНЫХ вопроса:**
   - Критичный = влияет на следующие действия
   - Например: "Что хотите делать сейчас: продолжить Step 3 или что-то другое?"

5. ⏸️ **ВАЖНО: Скажи явно:**
   ```
   ⏸️ Если сейчас не готовы отвечать - ничего страшного!
   Просто напишите "дальше" или "продолжай" когда будете готовы.
   ```

6. ⏳ **ЖДАТЬ ответа пользователя!**

---

## 🔄 Протокол "Cold Start" (Перезагрузка сессии)

> **Цель:** Минимизировать токены при восстановлении контекста

**При ПЕРВОМ сообщении после перезагрузки читать В ТАКОМ ПОРЯДКЕ:**

### Этап 1: Quick Status Check 🚀

1. ✅ **Read PROJECT_SNAPSHOT.md**
   - ПРИОРИТЕТ #1 - мгновенный обзор проекта
   - Текущая фаза, прогресс (%), статус модулей
   - Что сделано, что в работе, что дальше

### Этап 2: Context Loading (по необходимости)

2. ✅ **Read BACKLOG.md** - только текущий шаг!
   - BACKLOG.md = single source для чек-листов и задач
   - Читай ТОЛЬКО секцию текущего шага

3. ⏸️ **IF user asks to write code:**
   - Read ARCHITECTURE.md (только нужный модуль)
   - Read PIPELINE.md (state machine)

### Этап 3: Never Unless Asked

4. ❌ **НЕ читать автоматически:**
   - PROCESS.md (только если завершается фаза)
   - PROJECT_INTAKE.md (уже знаем проект)

---

## 🚀 Быстрый старт MaaS

### Ключевые файлы:
1. **PROJECT_SNAPSHOT.md** - ⭐ НАЧНИ ЗДЕСЬ - текущий статус
2. **BACKLOG.md** - план разработки по шагам (SINGLE SOURCE OF TRUTH)
3. **ARCHITECTURE.md** - архитектура системы (модули, схема БД)
4. **PIPELINE.md** - state machine и flow данных
5. **PROJECT_INTAKE.md** - требования и tech stack

### Tech Stack:
- **Runtime:** Node.js 18+ / TypeScript 5.3
- **Database:** Supabase (PostgreSQL managed)
- **LLM:** OpenAI API (gpt-4o-mini, gpt-4o)
- **HTTP Server:** Express 4.18
- **Architecture:** Event-Driven (LISTEN/NOTIFY)

---

## 📦 Команды

### npm scripts (package.json v0.2.1):
```bash
npm run dev          # Запустить HTTP сервер + Orchestrator (всё в одной команде!)
npm run test-runner  # Запустить Test Runner CLI
npm run build        # Скомпилировать TypeScript
npm run db:test      # Проверить подключение к БД
```

**Endpoints при запуске:**
- http://localhost:3000/ — API docs
- http://localhost:3000/emulator — User Emulator UI
- http://localhost:3000/test-runner — Test Runner UI
- http://localhost:3000/health — Health check (показывает статус Orchestrator)

### Git команды:
```bash
git status                    # Проверить статус
git diff                      # Посмотреть изменения
git log --oneline -n 10       # Последние 10 коммитов
```

---

## 🎨 Кодстайл

### TypeScript:
- ES-модули (import/export), не CommonJS
- Строгая типизация, избегать `any`
- Функциональный стиль где уместно

### Именование:
- camelCase для переменных и функций
- PascalCase для классов
- UPPER_SNAKE_CASE для констант

### Файловая структура (v0.2.0):
```
MaaS2/
├── maas/                    # PRODUCT (deliverable)
│   └── src/
│       ├── agents/          # Analyzer, Assembler, FinalResponder, Archivist
│       └── orchestrator/    # LISTEN/NOTIFY coordinator
│
├── learning-agent/          # TRAINING TOOL (not deliverable)
│   └── emulator/            # Test Runner → User Emulator
│       ├── src/             # engine.ts, api.ts, cli.ts
│       └── public/          # UI
│
├── shared/                  # Common infrastructure
│   ├── db.ts                # PostgreSQL connection
│   ├── logger.ts            # Logging
│   └── openai.ts            # OpenAI client
│
├── server.ts                # HTTP server entry point
├── main.ts                  # Orchestrator entry point
└── scripts/                 # Utility scripts
```

---

## 🔐 Безопасность

### Ключевые правила:
- ✅ Секреты только в `.env`, никогда в коде
- ✅ Параметризованные SQL запросы (защита от injection)
- ✅ Валидация входных данных
- ✅ `.env` в `.gitignore`
- ❌ Никогда не hardcode DATABASE_URL, OPENAI_API_KEY

### Environment Variables:
```env
DATABASE_URL=postgresql://...   # Supabase connection string
OPENAI_API_KEY=sk-...           # OpenAI API key
PORT=3000                       # HTTP server port
TEST_USER_ID=...                # UUID для тестов
```

---

## 📋 Database (Supabase/PostgreSQL)

### Таблицы:
1. **pipeline_runs** - State Machine (NEW → COMPLETED)
2. **lsm_storage** - Long-term Semantic Memory
3. **system_prompts** - Промпты для агентов
4. **raw_logs** - Сырой лог инференсов
5. **test_dialogs** - Тестовые сценарии
6. **test_runs** - Результаты тестов

### Триггер NOTIFY:
```sql
-- При изменении status в pipeline_runs
NOTIFY pipeline_events, '{"id": "...", "status": "NEW"}'
```

### Подключение:
- Используй `shared/db.ts`
- SSL включен для Supabase
- Connection pooling через `pg.Pool`

---

## 🏗️ Флоу работы

### Перед началом задачи:
1. Прочитай PROJECT_SNAPSHOT.md (текущий статус)
2. Прочитай BACKLOG.md (только текущий шаг)
3. Создай TodoWrite со списком задач

### Во время разработки:
1. Следуй паттернам из ARCHITECTURE.md
2. Обновляй TodoWrite по мере выполнения
3. Тестируй через Test Runner

### После завершения шага:
1. **⚠️ ОБЯЗАТЕЛЬНО:** Следуй PROCESS.md!
2. Обновить BACKLOG.md (отметить шаг ✅)
3. Обновить PROJECT_SNAPSHOT.md
4. Обновить эту секцию "🔄 Текущее состояние"
5. Git commit

---

## 🔧 Настройка окружения

### Требования:
- Node.js 18+
- npm
- Доступ к Supabase (DATABASE_URL)
- OpenAI API key

### Первый запуск:
```bash
npm install              # Установить зависимости
cp .env.example .env     # Создать .env
# Заполнить .env реальными значениями
npm run db:test          # Проверить подключение к БД
npm run dev              # Запустить сервер
```

---

## 🎯 Приоритеты разработки

1. **Работоспособность** - код должен работать
2. **Тестируемость** - каждый модуль тестируется через Test Runner
3. **Модульность** - независимые модули
4. **Документация** - обновлять метафайлы после каждого шага

---

## ⚠️ Предупреждения

- НЕ удаляй существующую функциональность без явного указания
- НЕ меняй архитектурные решения без обсуждения
- НЕ коммить закомментированный код (удаляй)
- НЕ игнорируй TypeScript ошибки - исправляй их
- **ВСЕГДА** обновляй метафайлы после завершения шага (PROCESS.md)

---

## 📝 Создание документов

**ОБЯЗАТЕЛЬНО перед созданием любого концептуального документа:**

1. ✅ **Прочитай [DOCS.md](./DOCS.md)** — инструкция по написанию документов
2. ✅ **Проверь, нужен ли документ** (секция 10 в DOCS.md)
3. ✅ **Используй правильный формат** для типа документа (секция 9)
4. ✅ **Избегай anti-patterns** (секция 11)

**Ключевой принцип:**
> Документ = спецификация артефактов и команд, НЕ презентация.
> Каждый абзац должен переводиться в: «Найди X, измени Y, ожидай Z».

**Чеклист перед завершением документа:**
- [ ] Есть конкретные артефакты (пути к файлам)?
- [ ] Есть конкретные действия (что менять)?
- [ ] Есть метрики/эффекты (как проверить)?
- [ ] Нет корпоративного языка и пустых метафор?

---

## 📚 Структура метафайлов

| Файл | Назначение | Когда обновлять |
|------|------------|-----------------|
| **CLAUDE.md** | Контекст для AI | После каждого шага |
| **PROJECT_SNAPSHOT.md** | Текущий статус | После каждого шага |
| **BACKLOG.md** | План разработки | После каждого шага |
| **PROCESS.md** | Процесс обновления | Не трогать |
| **ARCHITECTURE.md** | Архитектура | При изменении архитектуры |
| **PROJECT_INTAKE.md** | Требования | Редко (начало проекта) |
| **DOCS.md** | Как писать документы | При создании документов (читать!) |
| **ROADMAP.md** | План развития по фазам | При смене приоритетов |
| **IMPACTS.md** | Импакт-факторы | При изменении параметров системы |
| **METRICS.md** | Метрики системы | При работе с телеметрией |
| **docs/selflearn/** | Система самообучения | При развитии self-learning |
| **docs/memory/** | Архитектура памяти LSM | При изменении структуры памяти |

### Memory документы (docs/memory/):
| Файл | Назначение |
|------|------------|
| **LSM.md** | Основная спецификация памяти (требования + архитектура) |
| **PACKING.md** | Алгоритм семантической упаковки |
| **m_*.md** | Брейнсторминг-документы |

### Self-Learning документы (docs/selflearn/):
| Файл | Уровень | Назначение |
|------|---------|------------|
| **AGENT.md** | Agent | Mission Controller, стратегические решения |
| **MANAGER.md** | Sub-Agent | Cycle Coordinator, оркестрация |
| **ANALYST.md** | Sub-Agent | "Что не так?" — метрики, verdict |
| **TEACHER.md** | Sub-Agent | "Как исправить?" — гипотезы |
| **TUNER.md** | Sub-Agent | Применение параметров |
| **USER EMULATOR.md** | Sub-Agent | Генерация диалогов |
| **README.md** | Overview | Обзор архитектуры |

---

*Последнее обновление: 2025-11-29 (v0.2.1)*
*Следующий этап: MaaS Quality (улучшение использования memories)*

### Изменения v0.2.1:
- User Emulator v0.3.1: Pipeline/Direct modes, localStorage persistence
- Orchestrator интегрирован в server.ts (одна команда)
- Analyzer v0.2: LLM-based semantic keyword extraction
- Выявлена проблема: LLM не всегда активно использует retrieved memories
